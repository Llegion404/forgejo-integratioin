name: Publish Extension

on:
  push:
    tags:
      - 'v*.*.*'
      - '*.*.*'

jobs:
  publish:
    runs-on: docker
    container: node:20

    steps:
    - uses: actions/checkout@v4

    - name: Install xvfb and dependencies
      run: |
        apt-get update
        apt-get install -y xvfb libgtk-3-0 libxss1 libasound2 libgbm1 libnspr4 libnss3 jq

    - name: Install dependencies
      run: npm ci

    - name: Lint
      run: npm run lint

    - name: Run tests
      run: |
        npm run test:unit:coverage
        npm run compile
        xvfb-run -a npm run test:integration

    - name: Install vsce and ovsx
      run: npm install -g @vscode/vsce ovsx

    - name: Get version from tag
      id: get_version
      run: |
        # Remove 'v' prefix if present
        VERSION=${GITHUB_REF#refs/tags/}
        VERSION=${VERSION#v}
        echo "version=$VERSION" >> $GITHUB_OUTPUT

        # Detect pre-release versions (alpha, beta, rc)
        if echo "$VERSION" | grep -qE '-(alpha|beta|rc)([.-]|$)'; then
          echo "is_prerelease=true" >> $GITHUB_OUTPUT
          echo "Publishing pre-release version: $VERSION"
        else
          echo "is_prerelease=false" >> $GITHUB_OUTPUT
          echo "Publishing stable version: $VERSION"
        fi

    - name: Verify version matches package.json
      run: |
        PACKAGE_VERSION=$(node -p "require('./package.json').version")
        TAG_VERSION="${{ steps.get_version.outputs.version }}"
        if [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
          echo "Error: package.json version ($PACKAGE_VERSION) does not match tag version ($TAG_VERSION)"
          exit 1
        fi
        echo "Version verified: $PACKAGE_VERSION"

    - name: Package extension
      run: npm run package

    - name: Publish to VS Code Marketplace
      env:
        VSCE_PAT: ${{ secrets.VSCE_PAT }}
      run: |
        if [ "${{ steps.get_version.outputs.is_prerelease }}" = "true" ]; then
          echo "Publishing as pre-release..."
          vsce publish --pre-release -p $VSCE_PAT --baseContentUrl https://codeberg.org/maxking/forgejo-vscode/raw/branch/master --baseImagesUrl https://codeberg.org/maxking/forgejo-vscode/raw/branch/master
        else
          echo "Publishing as stable release..."
          vsce publish -p $VSCE_PAT --baseContentUrl https://codeberg.org/maxking/forgejo-vscode/raw/branch/master --baseImagesUrl https://codeberg.org/maxking/forgejo-vscode/raw/branch/master
        fi

    - name: Publish to Open VSX Registry
      env:
        OVSX_PAT: ${{ secrets.OPEN_VSX_PAT }}
      run: |
        # Open VSX uses publisher 'abhilashraj' - repackage with correct publisher
        jq '.publisher = "abhilashraj"' package.json > package.tmp && mv package.tmp package.json
        vsce package --baseContentUrl https://codeberg.org/maxking/forgejo-vscode/raw/branch/master --baseImagesUrl https://codeberg.org/maxking/forgejo-vscode/raw/branch/master
        OVSX_VSIX=$(ls -t *.vsix | head -1)
        if [ "${{ steps.get_version.outputs.is_prerelease }}" = "true" ]; then
          echo "Publishing to Open VSX as pre-release..."
          ovsx publish "$OVSX_VSIX" --pre-release -p $OVSX_PAT
        else
          echo "Publishing to Open VSX as stable release..."
          ovsx publish "$OVSX_VSIX" -p $OVSX_PAT
        fi
        # Restore original publisher
        git checkout package.json

    - name: Upload VSIX artifact
      uses: actions/upload-artifact@v3
      with:
        name: forgejo-vscode-${{ steps.get_version.outputs.version }}.vsix
        path: '*.vsix'
        retention-days: 90

    - name: Create Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Extract release notes from tag annotation if available
        TAG_MESSAGE=$(git tag -l --format='%(contents)' ${GITHUB_REF#refs/tags/})
        if [ -z "$TAG_MESSAGE" ]; then
          TAG_MESSAGE="Release ${{ steps.get_version.outputs.version }}"
        fi

        # Create release using Forgejo API
        VSIX_FILE=$(ls *.vsix)

        # Create the release
        IS_PRERELEASE="${{ steps.get_version.outputs.is_prerelease }}"
        PRERELEASE_FLAG="false"
        if [ "$IS_PRERELEASE" = "true" ]; then
          PRERELEASE_FLAG="true"
        fi

        # Build JSON payload safely using jq to handle special characters in TAG_MESSAGE
        JSON_PAYLOAD=$(jq -n \
          --arg tag "${GITHUB_REF#refs/tags/}" \
          --arg name "v${{ steps.get_version.outputs.version }}" \
          --arg body "$TAG_MESSAGE" \
          --argjson prerelease "$PRERELEASE_FLAG" \
          '{tag_name: $tag, name: $name, body: $body, draft: false, prerelease: $prerelease}')

        RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
          "${{ github.server_url }}/api/v1/repos/${{ github.repository }}/releases" \
          -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          -H "Content-Type: application/json" \
          -d "$JSON_PAYLOAD")
        HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
        if [ "$HTTP_CODE" -ne 201 ]; then
          echo "Failed to create Forgejo release: HTTP $HTTP_CODE"
          echo "$RESPONSE" | head -n -1
          exit 1
        fi
        echo "Release created successfully"
